kind: Module
name: ionic-app
description: Ionic Frontend
type: container
services:
  - name: ionic-app
    ports:
      - name: http
        containerPort: 3000
    ingresses:
      - path: /
        port: http
    env:
      PORT: 3000
